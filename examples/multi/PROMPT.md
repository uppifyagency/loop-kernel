# Task: implement three functions in `solution.py`

You are ONE iteration of an autonomous loop. The score is the FRACTION of tests passing,
so you can make partial progress and the loop will watch it climb.

## Goal (loop halts here)
All tests pass → `score=1.0000`.

## Implement (each currently raises NotImplementedError)
- `roman_to_int(s)`  — Roman numeral string → int
- `is_palindrome(s)` — True iff `s` reads the same reversed
- `fib(n)`           — nth Fibonacci number (fib(1)=1, fib(10)=55)

## Discipline — one thing per iteration
- CONSULT the LEDGER below first.
- Implement ONE function per iteration, then stop and let the loop re-score. Watch the score rise.
- Do NOT edit `tests/` or `score.py` (the kernel re-runs the real tests regardless).
